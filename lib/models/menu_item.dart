import 'package:flutter/cupertino.dart';

class MenuItem{
  IconData icon;
  String iconText;
  Color iconColor;
  Color textColor;

  MenuItem({required this.icon, required this.iconText, required this.iconColor, required this.textColor});
}